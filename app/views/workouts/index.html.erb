<h1 class="text-center mt-4">Listing of all Workouts</h1>

<div class="flickr_pagination">
  <%= will_paginate @workouts, :container => false %>
</div>
<%= render 'workout' %>
<div class="flickr_pagination mb-4">
  <%= will_paginate @workouts, :container => false %>
</div>
