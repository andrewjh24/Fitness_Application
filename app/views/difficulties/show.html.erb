<h1 class="text-center mt-4">Difficulty: <%= @difficulty.name %></h1>

  <% if logged_in? && current_user.admin? %>
  <div class="text-center mt-4">
    <%= link_to "Edit difficulty name", edit_difficulty_path(@difficulty), class: "btn btn-outline-info" %>
  </div>
  <% end %>

<h3 class="text-center mt-4">Workouts</h3>

<div class="flickr_pagination">
  <%= will_paginate @workouts, :container => false %>
</div>

<%= render 'workouts/workout' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @workouts, :container => false %>
</div>
